services:
  docs:
    image: template_local_docs
    container_name: template_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./template:/app/template:z
    ports:
      - '9000:9000'
    command: /start-docs
