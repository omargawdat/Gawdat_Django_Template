# ==============================================================================
# Django
# ==============================================================================
DJANGO_SECRET_KEY=your-secure-secret-key-here
DJANGO_SUPERUSER_USERNAME=omar
DJANGO_SUPERUSER_PASSWORD=123
DJANGO_ADMIN_NAME=Admin User
DJANGO_ADMIN_URL=admin/
DJANGO_ADMIN_EMAIL=admin@example.com

# ==============================================================================
# Database
# ==============================================================================
DATABASE_URL=postgis://postgres:postgres@postgres:5432/mydatabase

# Configurations
COMPOSE_BAKE=true

# AWS Settings
AWS_REGION_NAME=eu-central-1

# External Services
API_KEY=dummy-api-key-for-local-development
TAPS_SECRET_KEY=dummy-taps-secret-key
PAYMENT_CONFIRMATION_KEY=dummy-tap

# ==============================================================================
# Payment - Paymob
# ==============================================================================
PAYMOB_SECRET_KEY=your-paymob-secret-key-here
PAYMOB_PUBLIC_KEY=your-paymob-public-key-here
PAYMOB_CARD_PAYMENT_METHOD=123456
PAYMOB_WALLET_PAYMENT_METHOD=789012


# ==============================================================================
# Environment
# ==============================================================================
DOMAIN_NAME=localhost
ENVIRONMENT=local
SENTRY_SDK_DSN=https://dummy@sentry.example.com/123

# ==============================================================================
# AWS
# ==============================================================================
AWS_REGION_NAME=eu-central-1
S3_BUCKET_NAME=local-development-bucket

# ==============================================================================
# Firebase
# ==============================================================================
FIREBASE_CREDENTIALS_B64='dummuy-value'

# ==============================================================================
# Google Services
# ==============================================================================
GOOGLE_MAP_API_KEY='dummuy-value'

# ==============================================================================
# Payment
# ==============================================================================
TAPS_SECRET_KEY=dummy-taps-secret-key

# ==============================================================================
# SMS
# ==============================================================================
IS_TESTING_SMS=True
OUR_SMS_API_KEY=DUMMY_API_KEY
OUR_SMS_SENDER_NAME=DummySender
SMS_MISR_USERNAME=dummy-username-uuid
SMS_MISR_PASSWORD=dummy-password-hexstring
SMS_MISR_SENDER=dummy-sender-hexstring

# ==============================================================================
# Email
# ==============================================================================
EMAIL_HOST_USER=useradmin
EMAIL_HOST_PASSWORD=passwordadmin

# ==============================================================================
# Frontend Configuration (Django-Allauth Headless & CORS/CSRF)
# ==============================================================================
# Default frontend URL for email links and OAuth callbacks
# This is where users will be redirected from emails and after OAuth login
#
# Local Development:
FRONTEND_DEFAULT_URL=http://localhost:3000
#
# Dev/Staging Server:
# FRONTEND_DEFAULT_URL=https://dev-app.example.com
#
# Production:
# FRONTEND_DEFAULT_URL=https://app.example.com
#
# Mobile Apps (deep links):
# FRONTEND_DEFAULT_URL=myapp://auth
# This generates links like: myapp://auth/password/reset/key/{key}

# Comma-separated list of allowed frontend origins (for CORS and CSRF)
# These origins can make authenticated API requests to the backend
#
# CRITICAL for cross-origin development:
# When backend is deployed (dev/staging), frontend developers need to work
# locally (localhost:3000) against the deployed backend. To enable this,
# INCLUDE localhost URLs in dev/staging environments!
#
# Local Development (both frontend and backend local):
FRONTEND_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000,http://127.0.0.1:8000
#
# Dev/Staging Server (deployed backend, allows local frontend development):
# FRONTEND_ALLOWED_ORIGINS=https://dev-app.example.com,https://dev-api.example.com,http://localhost:3000,http://127.0.0.1:3000
# This allows frontend devs to set REACT_APP_API_URL=https://dev-api.example.com
# and develop locally while calling the deployed dev backend!
#
# Production (strict - only production URLs):
# FRONTEND_ALLOWED_ORIGINS=https://app.example.com,https://example.com

# Cookie domain for cross-subdomain authentication
# Controls where session/CSRF cookies are accessible
#
# Local Development:
COOKIE_DOMAIN=
# Leave EMPTY for localhost (cookies work without domain restriction)
#
# Dev/Staging Server:
# COOKIE_DOMAIN=
# Leave EMPTY if developers test from localhost (cookies won't work with domain set)
#
# Production (same domain):
# COOKIE_DOMAIN=example.com
# Backend at example.com/api, Frontend at example.com
#
# Production (subdomains):
# COOKIE_DOMAIN=.example.com
# Backend at api.example.com, Frontend at app.example.com
# Leading dot (.) allows cookies to work across ALL subdomains
#
# IMPORTANT: Cookie domain MUST match browser's current domain or subdomain!
# Setting .example.com when accessing from localhost will BREAK cookies!

# ==============================================================================
# OAuth Providers
# ==============================================================================
GOOGLE_OAUTH2_CLIENT_ID=your-google-client-id-here
GOOGLE_OAUTH2_CLIENT_SECRET=your-google-client-secret-here

# ==============================================================================
# Other
# ==============================================================================
API_KEY=dummy-api-key-for-local-development
COMPOSE_BAKE=true
